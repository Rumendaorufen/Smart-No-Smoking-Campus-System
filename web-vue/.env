# 使用相对路径，触发 Vite 的代理功能
VITE_API_BASE_URL=/api/v1

# 1. 业务后端 (Spring Boot) -> 负责登录、列表、报警数据、WebSocket
VITE_APP_BASE_API = 'http://localhost:8080'

# 2. 视觉后端 (Python) -> 负责视频流、录像回放、图片加载
VITE_APP_AI_API = 'http://localhost:5000'